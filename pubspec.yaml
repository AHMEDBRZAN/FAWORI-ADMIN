name: fawori_admin
description: FAWORI Admin
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.0
  file_picker: ^8.1.4
  excel: ^4.0.6

flutter:
  uses-material-design: true
